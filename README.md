# Shadowvale Clan Site

Multi-page static site with shared CSS fixed.

## Run locally
Open `index.html` directly or use Vite.

```bash
npm install
npm run dev
```

## Push fix
```bash
git add .
git commit -m "Fix multipage CSS and shared layout"
git push
```
